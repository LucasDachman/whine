{
  "name": "whine",
  "version": "1.0.0",
  "description": "whiner",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "watchify src/*.js -o src/bundle.js"
  },
  "author": "lucas",
  "license": "MIT",
  "dependencies": {
    "mouse-speed": "^2.0.1"
  },
  "devDependencies": {
  }
}
